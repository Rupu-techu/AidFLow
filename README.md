# Agentic Volunteer Intelligence

A Multi-Agent AI system designed to intelligently parse unstructured emergency text, prioritize the urgency, classify the need, and assign the best possible volunteer match.

## Getting Started

1. Set up a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Or `venv\Scripts\activate` on Windows
