/**
 * Contains JSDoc typedefs to provide type-hinting in the JS codebase.
 */

/**
 * @typedef {Object} ProcessPayload
 * @property {string} text - The main text summarizing the disaster report.
 */

export {};
